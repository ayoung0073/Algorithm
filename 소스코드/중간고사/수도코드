Alg findSmallerKeys(v, x)
    input heap with root v, key x
    output keys in the heap that are smaller than x

    empty list keys

    if (key(v) >= x) 
        return

    keys.add(v)
    
    if(!isExternal(leftChild(v)))
        keys.add(findSmallerKeys(v, leftChild(v)))

    if(!isExternal(rightChild(v)))
        keys.add(findSmallerKeys(v, rightChild(v)))
    
    return keys
    